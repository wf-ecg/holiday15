var model = {
    intro: '<div>',
    game: '<div>',
    share: '<div>',
};
var tile = {
    showing: false,
    letter: '',
};
var jumbles = {
    'abc': ['acb', 'bca'],
    'def': ['fed', 'fde'],
};
function play() {
    // hide intro
    // show game
    // pick random phrase
    // disply game layout
}
function makeLayout(phrase) {
    // take phrase
    // make tiles from correct version
    // show keys from jumbled version

}
